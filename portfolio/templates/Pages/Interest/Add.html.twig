{% extends 'Layout/Backoffice.html.twig' %}

{% block content %}

    <div class="well">
        <h2 class="text-center">{{ title }}</h2>

        {{ form_start(interestForm, { 'attr': {'class': 'form-horizontal'} }) }}
        <div class="form-group">
            {{ form_label(interestForm.label, NULL, { 'label_attr':  {
                'class': 'col-sm-4 control-label'
            }}) }}
            <div class="col-sm-6">
                {{ form_errors(interestForm.label) }}
                {{ form_widget(interestForm.label, { 'attr':  {
                    'class': 'form-control'
                }}) }}
            </div>
        </div>
        <div class="form-group">
            {{ form_label(interestForm.link, NULL, { 'label_attr':  {
                'class': 'col-sm-4 control-label'
            }}) }}
            <div class="col-sm-6">
                {{ form_errors(interestForm.link) }}
                {{ form_widget(interestForm.link) }}
                {{ form_widget(interestForm.bio) }}
            </div>
        </div>

        <div class="form-group">
            <div class="col-sm-offset-4 col-sm-3">
                <input type="submit" class="btn btn-primary" value="Save" />
            </div>
        </div>
        {{ form_end(interestForm) }}
    </div>

{% endblock %}