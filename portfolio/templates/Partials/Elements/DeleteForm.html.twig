{{ form_start(deleteForm, { 'attr': {'class': 'form-horizontal'} }) }}
    {{ form_widget(deleteForm.id) }}
    <input type="submit" class="btn btn-danger" value="Confirm" />
{{ form_end(deleteForm) }}